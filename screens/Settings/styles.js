import {
    StyleSheet
  } from "react-native";

const styles = StyleSheet.create({
    container: {
      backgroundColor: "#2F303F",
      paddingTop: 120,
      paddingBottom: 120,
      flex: 1, 
      justifyContent: 'center'
    },
    profileTab: {
        flex: 1,
        flexDirection: 'row',
    }
  });
  
export default styles;